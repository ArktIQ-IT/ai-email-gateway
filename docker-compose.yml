version: '3.9'
services:
  gateway:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./config/accounts.yaml:/app/config/accounts.yaml:ro
      - ./data:/app/data
    restart: unless-stopped
